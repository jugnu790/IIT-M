[project]
name = "iit-m"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "creator79", email = "upadhyayvivek.19@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "scikit-learn>=1.9.0",
    "torch>=2.13.0",
]

[project.scripts]
iit-m = "iit_m:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
[tool.uv]
package = false
